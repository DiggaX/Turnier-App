# Dependencies
node_modules/
# only the stray root lockfile (claude-flow install); the app lockfile in web/ IS tracked
/package-lock.json

# Ruflo / claude-flow: lokaler Clone + Runtime-Daten
/ruflo/
.claude-flow/
*.rvf
*.rvf.lock

# Skill-Installs (verwaltet via `npx skills`)
/.agents/

# Brainstorming Visual Companion
.superpowers/

# Secrets / Env (covers .env, .env.local, .env.md, etc.; keep example templates)
.env
.env.*
!.env.example
!**/.env.example
*.pem

# Next.js / Build
.next/
out/
dist/
build/
.vercel/

# Supabase
.branches/
.temp/

# OS / Editor
.DS_Store
Thumbs.db
*.log

# Design-Referenzen (HTML-Bundles, lokal)
design-refs/
.vercel
